language: python
python:
  - "3.5"
  - "3.6"
notifications:
  email: false
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - pytest
# coveralls
after_success:
  - coveralls